services:
  mysql:
    image: mysql:8.4
    container_name: idcard-mysql

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: idcard_db

    ports:
      - "3307:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: